const redHeaderButton = document.querySelector('#red_header');
const headerElement = document.querySelector('header');

reHeaderButton.addEventListener('click', () => {
  headerElement.classList.add('red');
});
